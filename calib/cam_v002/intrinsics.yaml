camera_id: cam_v002
model: pinhole   # or "opencv_pinhole"
image_size:
  width: 1920
  height: 1080

K:
  - [fx, 0.0, cx]
  - [0.0, fy, cy]
  - [0.0, 0.0, 1.0]

# Distortion (OpenCV)
# For standard: [k1, k2, p1, p2, k3]
D: [k1, k2, p1, p2, k3]

# Optional
newK: null        # if you computed optimal new camera matrix
roi: null         # if you used undistort cropping
